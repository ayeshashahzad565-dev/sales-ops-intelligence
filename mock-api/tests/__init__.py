"""Test package for the Mock Sales/Orders API.

This file is required, not incidental: without it `tests` would be a namespace
package, and Python's import machinery would happily resolve `tests.conftest`
to any other `tests` package that appears earlier on `sys.path` - which is
exactly what happens on a machine with another project editable-installed.
"""
